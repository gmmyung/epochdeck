# Runloom Python SDK

The Python client for Runloom, a standalone experiment tracker designed for
W&B-compatible workflows without hosted-service or Hugging Face dependencies.

The SDK is intentionally small while the compatibility surface is being built.
Its first stable contract is server discovery and health reporting; run
lifecycle, metric logging, media, artifacts, and public API compatibility follow
the milestones documented in the main repository.
